package main

import (
	"machine"
	"time"
)

const (

	// - WiFi --------------------------------------------------------------------

	// to disable, leave either empty

	// Wifi connectivity
	wifiSsid = "" // wifi access point name
	wifiPass = "" // wifi access point password

)
